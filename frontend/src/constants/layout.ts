/** Fixed width of the desktop app sidebar. */
export const APP_SIDEBAR_WIDTH = 108;

/** Width when the sidebar drawer is open on mobile. */
export const APP_SIDEBAR_WIDTH_MOBILE = 280;

/** Width of the agent chat history sidebar when expanded on desktop. */
export const CHAT_SIDEBAR_WIDTH = 300;

/** Collapsed chat sidebar rail — toggle stays visible when closed */
export const CHAT_SIDEBAR_RAIL_WIDTH = 64;
