/** App route paths — keep URLs aligned with the page they render. */
export const ROUTES = {
  agent: '/',
  dashboard: '/dashboard',
  profile: '/profile',
  songs: '/songs',
  editProfile: '/edit-profile',
  changePassword: '/change-password',
  login: '/login',
  signup: '/signup',
} as const;
