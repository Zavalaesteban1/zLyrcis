[phases.setup]
nixPkgs = ["python312", "python312Packages.pip", "ffmpeg"]

[phases.install]
cmds = ["python3 -m pip install -r backend/requirements.txt"]

[start]
cmd = "cd backend && python3 -m gunicorn lyric_video_project.wsgi:application --bind 0.0.0.0:$PORT"
